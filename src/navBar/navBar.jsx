import { useState } from "react";
import { Container, NavButton,FirstDiv, SecondDiv, ThirdDiv, Title } from "./styles";
const Navbar = ({cb}) => {
  return (
    <div className="w-screen h-0 sm:h-20 bg-gradient-to-br from-purple-600 to-blue-500 flex justify-end items-center">
    </div>
  );
};

export default Navbar;
